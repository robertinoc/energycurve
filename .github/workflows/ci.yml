name: CI

on:
  pull_request:
  push:
    branches: [main]

# A second push to the same branch makes the first run's answer irrelevant, and
# two full runs of this job take two runners for ~12 minutes to tell you one
# thing. `main` is exempt: its runs are the record of what was released.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: ${{ github.ref != 'refs/heads/main' }}

# Least privilege for GITHUB_TOKEN, declared here rather than relied on.
#
# The repository setting is already `default_workflow_permissions: read`, which
# is the right value — but that value lives in GitHub's UI, not in this repo,
# and nothing here would notice if it changed. This job reads code and writes
# artifacts; it never needs to push a commit, open an issue, or publish a
# package, so it says so in the file that gets reviewed.
#
# Worth being precise about what this protects against: every `npm ci` below
# executes install scripts from the whole dependency tree. A compromised
# transitive dependency runs as this job. With a read-only token it can read
# public code; with a write token it could push to `main`.
permissions:
  contents: read

jobs:
  verify:
    runs-on: ubuntu-latest
    # Without this the job's ceiling is GitHub's default of 6 hours. The browser
    # install below hung for 22 minutes on two pull requests at once, and a hung
    # check is worse than a failing one: nothing can merge and nothing says why.
    # Every green run so far finishes inside 6 minutes.
    timeout-minutes: 20
    env:
      # Placeholders only: routes are force-dynamic, so nothing connects to
      # these services at build time. They exist so config readers that
      # check for presence behave like a configured environment.
      WORKOS_CLIENT_ID: client_ci_placeholder
      WORKOS_API_KEY: sk_test_ci_placeholder
      WORKOS_COOKIE_PASSWORD: ci_placeholder_cookie_password_32chars!
      NEXT_PUBLIC_WORKOS_REDIRECT_URI: http://localhost:3010/auth/callback
      SUPABASE_URL: https://ci-placeholder.supabase.co
      SUPABASE_SERVICE_ROLE_KEY: ci_placeholder_service_role_key
      # Analytics has to be *configured* for the consent tests to mean anything:
      # without a key the tracker no-ops, and "no PostHog cookie after accepting"
      # would pass for the wrong reason. The host points at port 9 (discard), so
      # the key exists, cookies are set on init, and not one request leaves the
      # runner.
      NEXT_PUBLIC_POSTHOG_KEY: phc_ci_placeholder
      NEXT_PUBLIC_POSTHOG_HOST: http://127.0.0.1:9
    steps:
      # Third-party actions are pinned to a commit SHA, not a tag.
      #
      # `@v4` is a *mutable* pointer: the owner can move it, and whoever
      # controls the tag controls what runs inside this job — with the
      # repository checked out and the token in scope. Pinning the SHA means an
      # upgrade is a reviewable diff instead of something that happens to a run.
      #
      # The cost is that these do not update themselves, which is why
      # .github/dependabot.yml now watches them: a pin nobody bumps becomes a
      # different supply-chain problem six months later.
      #
      # `persist-credentials: false` because the very next step runs `npm ci`.
      # By default checkout leaves the token in .git/config, where any install
      # script in the dependency tree can read it and push with it.
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262 # v4.4.0
        with:
          persist-credentials: false

      - uses: actions/setup-node@49933ea5288caeca8642d1e84afbd3f7d6820020 # v4.4.0
        with:
          node-version: 22
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint

      - name: Typecheck
        run: npm run typecheck

      - name: Unit tests with coverage
        # Thresholds live in vitest.config.ts, including per-directory floors.
        # They are set to what has already been cleared, so this fails only on a
        # real drop — and the floors are raised as coverage lands, never lowered
        # to make a run pass.
        run: npm run test:coverage

      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: coverage
          path: coverage/
          retention-days: 14

      # Advisory on purpose. A new critical advisory in a transitive dependency
      # would otherwise block every unrelated pull request until someone upstream
      # publishes a fix, and a gate that has to be bypassed to ship stops being
      # read. The output is here to be looked at; the decision stays human.
      - name: Dependency audit (advisory)
        continue-on-error: true
        run: npm audit --audit-level=high

      # Custom rules for this codebase's own invariants — the ownership scope on
      # playlist queries, the unscoped loader's sanctioned callers, raw JSON in
      # a script tag. Not the registry ruleset: without a login it loads two
      # rules for JavaScript, and neither catches a planted command injection.
      # Verified with a canary before being trusted.
      - name: Install Semgrep
        # Pinned for the same reason the actions above are: this installs an
        # executable from PyPI and runs it over the whole repository. Unpinned,
        # the version that gates a merge is whatever was published that morning,
        # and a rule-behaviour change arrives as a red build on an unrelated PR.
        run: pipx install semgrep==1.177.0

      - name: Static analysis
        # ERROR severity only, so the gate blocks on a real invariant break and
        # the WARNING findings stay visible in the log without failing a build.
        run: npm run sast

      - name: Production build
        run: npm run build

      # After the build, reusing its output: the e2e suite runs against `next start`,
      # not `next dev`, because it asserts on rendered metadata and structured data
      # and dev-mode output is not what ships.

      # Keyed on the resolved Playwright version **and the browser set**, not on
      # the lockfile hash: the browser build only changes when Playwright does,
      # so an unrelated dependency bump shouldn't throw away a 300 MB download.
      #
      # The browser set belongs in the key, and leaving it out cost a full red
      # run. When webkit and firefox were added to the matrix, the key was still
      # version-only — so the cache saved by an earlier chromium-only run still
      # hit, the install step was skipped as a cache hit, and 126 tests failed
      # with "Executable doesn't exist". Locally everything passed, because a
      # local cache already had all three.
      #
      # Defined once and used for both the key and the install command, so the
      # two can't drift apart again.
      - name: Resolve Playwright version
        id: playwright
        run: |
          BROWSERS="chromium webkit firefox"
          echo "version=$(node -p "require('@playwright/test/package.json').version")" >> "$GITHUB_OUTPUT"
          echo "browsers=$BROWSERS" >> "$GITHUB_OUTPUT"
          # Derived, never written twice: the cache key and the install command
          # have to name the same set or the cache lies.
          echo "browserkey=$(echo "$BROWSERS" | tr ' ' '-')" >> "$GITHUB_OUTPUT"

      - name: Cache Playwright browsers
        id: playwright-cache
        uses: actions/cache@0057852bfaa89a56745cba8c7296529d2fc39830 # v4.3.0
        with:
          path: ~/.cache/ms-playwright
          key: playwright-${{ runner.os }}-${{ steps.playwright.outputs.version }}-${{ steps.playwright.outputs.browserkey }}

      # Two steps, and the split is the whole point.
      #
      # `actions/cache` restores `~/.cache/ms-playwright`, which holds the browser
      # *binaries*. It cannot hold WebKit's system libraries — libevent, libwoff2,
      # libopus, gstreamer, libenchant and friends live in /usr/lib and are not in
      # the runner image. So a combined `install --with-deps` behind a cache-hit
      # guard is wrong in a way that only shows up on the second run: the first
      # run misses the cache, installs both, and passes; every run after it hits
      # the cache, skips the step entirely, and WebKit launches into
      # "error while loading shared libraries: libevent-2.1.so.7".
      #
      # That is exactly how this failed. PR #181 was green on a fresh cache key
      # and PR #182 was red on the same code.
      #
      # So: system dependencies every run, browser downloads only on a miss.
      #
      # `install-deps` shells out to apt, which is what hung CI for 22 minutes
      # once — it blocks on a lock the runner's own unattended-upgrades holds.
      # Stopping that timer first addresses the actual cause, and the step
      # timeout is the backstop: if it ever hangs again it fails in five minutes
      # with a name instead of twenty-two without one.
      - name: Install browser system dependencies
        timeout-minutes: 5
        run: |
          sudo systemctl stop unattended-upgrades.service 2>/dev/null || true
          sudo systemctl disable --now apt-daily.timer apt-daily-upgrade.timer 2>/dev/null || true
          npx playwright install-deps ${{ steps.playwright.outputs.browsers }}

      - name: Install Playwright browsers
        if: steps.playwright-cache.outputs.cache-hit != 'true'
        timeout-minutes: 8
        run: npx playwright install ${{ steps.playwright.outputs.browsers }}

      - name: End-to-end tests
        timeout-minutes: 12
        run: npm run test:e2e

      # Only on failure, and only then: a trace is tens of megabytes and is
      # worth nothing for a run that passed. `trace: retain-on-failure` in
      # playwright.config.ts means this directory is empty unless something
      # actually broke — which is exactly when reproducing it locally is hardest.
      - name: Upload Playwright report
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: playwright-report
          path: |
            playwright-report/
            test-results/
          retention-days: 14
